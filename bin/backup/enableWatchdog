#!/bin/bash
echo "enabling watchdog in cron"
if crontab -l | grep --quiet "/var/akira/bin/watchdog"; then
  echo " - cron already configured"
else
  crontab -l | { cat; echo "*/1 * * * * /var/akira/bin/watchdog"; } | crontab -
fi
