#!/usr/bin/python3
import subprocess
import sys
from datetime import datetime

import akiraCore

#print(akiraCore.readers)

for reader in akiraCore.readers:
    cmd = akiraCore.BIN_DIR + "/reader"
    subprocess.Popen([cmd, reader['name']])

