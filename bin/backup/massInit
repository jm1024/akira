#!/usr/bin/python3
import subprocess
import sys
from datetime import datetime

import sidraCore

print(sidraCore.massSensors)

for mass in sidraCore.massSensors:
    cmd = sidraCore.BIN_DIR + "/mass"
    subprocess.Popen([cmd, mass['name']])

