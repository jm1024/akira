#!/bin/bash
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
AKIRA_ROOT="${AKIRA_ROOT:-$(dirname -- "$SCRIPT_DIR")}"
#if [$1 -eq 0]
#then
#    echo no lane
#    tail -f "$AKIRA_ROOT/log/akira.log"
#else
#    echo lane
    tail -f "$AKIRA_ROOT/log/akira.log" | grep "lane:$1"
#fi
