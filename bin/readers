#!/usr/bin/python3
import subprocess
import sys
from datetime import datetime

import sidraCore

#print(sidraCore.readers)

for reader in sidraCore.readers:
    cmd = sidraCore.BIN_DIR + "/reader"
    subprocess.Popen([cmd, reader['name']])

